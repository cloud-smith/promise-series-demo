import React from 'react';
import {
  ArraySeries,
  NamedSeries,
} from '../../examples';

export const contents: any = {
  'array-series': () => <ArraySeries />,
  'named-series': () => <NamedSeries />,
};
